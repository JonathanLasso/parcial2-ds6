<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Estilo Base para Modo Claro (Fresco y Profesional) -->
    <style name="Base.Theme.GestionOrdenesCompras" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorOnPrimary">@color/on_primary_light</item>
        <item name="android:colorBackground">@color/background_light</item>
        <item name="android:textColorPrimary">@color/text_primary_light</item>
        <item name="android:textColorSecondary">@color/text_secondary_light</item>
        <item name="colorError">@color/error_light</item>
    </style>

    <!-- Tema final que lee Android Studio y el Manifiesto -->
    <style name="Theme.GestionOrdenesCompras" parent="Base.Theme.GestionOrdenesCompras" />
</resources>